```
Simple project with gin web framework
```


```
This project contains two server. HTTP server and GRPC server.

HTTP server only receive HTTP request, invoke GRPC service and return HTTP response. It doesn't connect to database or cache.

All works are done in GRPC server. 
```
